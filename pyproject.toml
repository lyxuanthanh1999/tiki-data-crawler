[tool.poetry]
name = "tiki-data-crawler"
version = "0.1.0"
description = "Selenium Worker Hybrid crawler for Tiki product-detail data."
authors = ["lyxuanthanh"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
aiohttp = ">=3.9.0"
tqdm = ">=4.66.0"
selenium = ">=4.23.0"

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
